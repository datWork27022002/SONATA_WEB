import routes from './routes';
import api from './api';
import language from './language';
import menuStructure from './menuStructure';

const config = { routes, api, language, menuStructure };

export default config;
