const routes = {
    LOGIN: '/login',
    HOME: '/',
    BasicSettings: {
        BASICSETTINGS: '/BasicSettings',
    },
    ManagerFunction: {
        MANAGERFUNCTION: '/ManagerFunction',
    },
};

export default routes;
